<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/output.css">
    <title><%= title %></title>
</head>
<body class="min-h-screen flex items-center justify-center bg-gray-50">
    <div class="w-full max-w-md p-6 bg-white rounded shadow">
        <h1 class="text-4xl text-center mb-6">Signup</h1>
        <form action="/auth/register" method="POST" class="flex flex-col gap-4 items-center">
            <div class="w-full">
                <label for="firstName">First Name: </label>
                <input type="text" name="firstName" id="firstName" placeholder="First name" required class="w-full border px-3 py-2 rounded">
            </div>
            
            <div class="w-full">
                <label for="lastName">Last Name: </label>
                <input type="text" name="lastName" id="lastName" placeholder="Last name" required class="w-full border px-3 py-2 rounded">
            </div>
            
            <div class="w-full">
                <label for="email">Email: </label>
                <input type="email" name="email" id="email" placeholder="Email" required class="w-full border px-3 py-2 rounded">
            </div>
            
            <div class="w-full">
                <label for="pw">Password: </label>
                <input type="password" name="pw" id="pw" placeholder="Password" required class="w-full border px-3 py-2 rounded">
            </div>

            <div class="w-full">
                <label for="confirmPw">Confirm Password: </label>
                <input type="password" name="confirmPw" id="confirm-pw" placeholder="Confirm Password" required class="w-full border px-3 py-2 rounded">
            </div>
            
            <button type="submit" class="font-semibold text-xl hover:cursor-pointer">Submit</button>
            <a href="/" class="inline-block text-center bg-gray-200 text-gray-700 px-4 py-2 rounded hover:bg-gray-300 text-sm">
                Cancel
            </a>
        </form>
    </div>

    <span id="success-msg-data" class="hidden"><%= success_msg || "" %></span>
    <span id="error-msg-data"   class="hidden"><%= error_msg   || "" %></span>




    <script src="/vendor/toastify/toastify.js"></script>
    <script src="/js/toastLogic.js"></script>


    <script>
        window.addEventListener("DOMContentLoaded", () => {
            const form = document.querySelector('form');
            const pw = document.getElementById('pw');
            const confirmPw = document.getElementById('confirm-pw');

            form.addEventListener('submit', (e) => {
                if (pw.value !== confirmPw.value) {
                    e.preventDefault();
                    alert('Passwords do not match.');
                    confirmPw.focus();
                }
            });
        });
    </script>

</body>
</html>